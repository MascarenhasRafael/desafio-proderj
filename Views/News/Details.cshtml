@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@model desafio_proderj.Models.New

@{
    ViewData["Title"] = "Notícia";
}

<fieldset>
    <div class="d-flex justify-content-between">
        <legend>@Html.DisplayFor(model => model.Title)</legend>
        <div class="d-flex align-items-center">
            @if (SignInManager.IsSignedIn(User)) {
                <a asp-action="Edit" asp-route-id="@Model.ID" class='btn btn-secondary'>
                    Editar
                </a>
            }
            <a asp-action="Index" class='btn btn-light'>
                Voltar
            </a>
        </div>
    </div>
    <hr />

    <div class="col-md-10">
        <img src="@("~/image/" + Model.ImageName)" asp-append-version="true" class="news-image" />
        @Html.DisplayFor(model => model.Content)
    </div>
</fieldset>

